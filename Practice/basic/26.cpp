#include <iostream>

using namespace std;
int main()
{
    int b1, b2;
    cout << "Enter the first Binary no: ";
    cin >> b1;
    cout << "Enter the second Binary no: ";
    cin >> b2;

    while (b1 != 0 && b2 != 0) {
        if 
        b1 / 10;
        b2 / 10;
    }
    return 0;
}
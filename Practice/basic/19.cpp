#include <iostream>

using namespace std;
int main()
{
    string fn, ln;
    cout << "Enter the first name: ";
    cin >> fn;
    cout << "Enter the last name: ";
    cin >> ln;
    cout << "The Reversed name: " << ln << " " << fn << endl;

    return 0;
}